{
  "name": "media-talk",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend-next\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend-next",
    "build": "npm run build --prefix frontend-next",
    "install:all": "npm install --prefix backend && npm install --prefix frontend-next"
  },
  "dependencies": {
    "next": "16.1.7"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "description": "updated backend"
}
